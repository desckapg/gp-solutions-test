spring:
  application:
    name: gp-solutions-test
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:h2:mem:hotels;DB_CLOSE_DELAY=-1
  jackson:
    property-naming-strategy: LOWER_CAMEL_CASE
    default-property-inclusion: non_null

server:
  port: 8092
